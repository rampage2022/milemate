/** Layout tokens for approved Add Stop screen (`designs/approved/2_add_stop_screen.png`). */
export const AddStopLayout = {
  headerTitleSize: 28,
  searchRadius: 14,
  quickActionCardWidth: 132,
  quickActionCardHeight: 112,
  rowIconSize: 40,
  addButtonSize: 36,
  sectionTitleSize: 17,
  footerActionSize: 15,
} as const;
