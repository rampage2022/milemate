/** Layout tokens for Workday Preview (approved mock 4). */
export const WorkdayPreviewLayout = {
  headerTitleSize: 17,
  greetingTitleSize: 22,
  greetingSubtitleSize: 15,
  mapHeightRatio: 0.36,
  mapMinHeight: 220,
  mapMaxHeight: 280,
  mapRadius: 16,
  statValueSize: 22,
  statLabelSize: 14,
  statSubtitleSize: 12,
  statIconSize: 40,
  sectionTitleSize: 16,
  checklistItemSize: 14,
  footerCaptionSize: 12,
  contentGap: 14,
  statGridGap: 10,
} as const;
